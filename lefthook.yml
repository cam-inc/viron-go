pre-push:
  commands:
    test:
      run: task test
pre-commit:
  commands:
    lint:
      run: task lint
    scripts:
      validate-commit-msg:
        runner: bash
        script: ./scripts/validate-commit-msg.sh
        args: ["{1}"]
