pre-commit:
   parallel: true
   commands:
      golangci-lint-lib:
        run: |
          cd lib && ./../bin/golangci-lint run  ./...  -v
      golangci-lint-example:
        run: |
          cd example && ./../bin/golangci-lint run  ./...  -v
