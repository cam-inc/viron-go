commit-msg:
  scripts:
    validate-commit-msg:
      runner: bash
      script: ./scripts/validate-commit-msg.sh

pre-commit:
  commands:
    lint:
      run: task lint

pre-push:
  commands:
    test:
      run: task test